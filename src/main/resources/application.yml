micronaut:
  router:
    static-resources:
      swagger:
        paths: classpath:META-INF/swagger
        mapping: /swagger/**
  application:
    name: alticci
netty:
  default:
    allocator:
      max-order: 3
